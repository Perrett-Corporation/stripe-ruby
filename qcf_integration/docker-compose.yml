version: '3.8'
services:
  qcf:
    build: .
    ports:
      - "4567:4567"
    environment:
      - ADMIN_USER=admin
      - ADMIN_PASS=change-me
      - PERRETT_WEBHOOK_URL=${PERRETT_WEBHOOK_URL}
      - SUPREGTECH_WEBHOOK_URL=${SUPREGTECH_WEBHOOK_URL}
    volumes:
      - ./qcf.db:/app/qcf.db
